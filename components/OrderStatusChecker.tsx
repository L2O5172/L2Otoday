
import React from 'react';

const OrderStatusChecker: React.FC = () => {
    // This component is disabled and renders nothing to prevent errors from
    // calling a non-existent 'checkOrderStatus' API endpoint.
    return null;
};

export default OrderStatusChecker;
